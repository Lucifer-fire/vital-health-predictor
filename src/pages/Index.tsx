import React from 'react';

// This file is no longer needed as we've moved all routing logic to App.tsx
// Keeping it empty to avoid any import errors
const Index = () => {
  return null;
};

export default Index;
